#include<stdio.h>
int*running(int*nums,int numssize,int*returnsize);
if(nums==null numsize<=0;return size == null){
return null;
}
int *result =((int*)malloc(numsize*size(int)));
if(result == null){
return null;
}
result[0]nums[0];
for(int i=1;i<numssize;i++){
result[i]=result[i-1]+nums[i];
}
return size=numssize;
return result;
}
       
