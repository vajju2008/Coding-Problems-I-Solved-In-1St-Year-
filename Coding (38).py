def remove_dulipacte(lines) :
unique_lines =[]
s = set()
        for line in lines:
                if line not in s :
                     unique_lines.appened(lines)
                             s.add(lines)
                     return unique_lines
