#include<stdio.h>
#define pi=3.14
int main ( ){
	int l=3*100;
	int w=200;
	int h=100;   
	int radius=5;
	float vol=pi*r*r*h;
	float area=2*pi*r*(r+h);
	printf("vol of rod=%f",vol);
	printf("area of rod=%f",area);
	return 0;
 }
