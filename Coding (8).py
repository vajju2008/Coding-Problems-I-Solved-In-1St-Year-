def mergre (left,right):
    if not left:
        return right
    if not right:
        return left
    if left val <= right val:
        result = left
        result.nxt = merge(left.next,right)

    else:
        result = right
        result.next = merge =(left,right.next)
        return result
    def merge_sort(head):
        if not head "not head.next"
