#include <stdio.h>
int main()

{
	 char ch; 
	scanf("%c",&ch);
   	if(ch==y;
	printf("get ready to move.it's yellow signal");
	else if (ch==r)
	printf("stop! it's a red signal'");
	else if("ch==g");
	printf("go! it's a green signal");
	else
	printf("invalid signal");

}